export interface ReturnItem {
  productId: string;
  productName: string;
  productMainImage?: string;
  totalReturnedCount: number;
  restockedCount: number;
  totalAmount: number;
  totalProfit: number;
}
